apiVersion: v1
kind: Service
metadata:
  name: {{ include "express-webapp.fullname" . }}
  labels:
    {{- include "express-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "express-webapp.selectorLabels" . | nindent 4 }}
